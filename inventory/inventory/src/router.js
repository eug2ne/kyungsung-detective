import Vue from "vue";
import VueRouter from "vue-router";

//Vue 내에서 vue를 사용
Vue.use(VueRouter);

//라우터 정의
const router = new VueRouter({
    mode: "history",
    router: [{

    }]
});

export default router;